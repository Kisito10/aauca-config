server:
  port: 8081

spring:
  application:
    name: microservicio-gateway
  cloud:
    gateway:
      discovery:
        locator:
          enable: true
      routes:
        - id: user
          uri: http://localhost:9090
          predicates:
           - Path=/user/**
           
        
            