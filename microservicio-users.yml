server:
  port: 9090

spring:
  application:
    name: microservicio-users
  
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://reseau.proxy.rlwy.net:50065/railway?useSSL=false&serverTimezone=UTC
    username: root
    password: EAyUBbUHuOsQRhZyXWQOYhzjmaXhsOPM
    type: com.zaxxer.hikari.HikariDataSource
    hikari:
      maximum-pool-size: 5
  
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
  
  sql:
    init:
      mode: always
  
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME:aaucanews@gmail.com}
    password: ${MAIL_PASSWORD:vwwg zved ezwe nbes}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 10000
          writetimeout: 5000
  
  security:
    user:
      name: disabled
      password: disabled
  
  web:
    resources:
      static-locations: file:./uploads/,classpath:/static/
      add-mappings: true

jwt:
  secret: ${JWT_SECRET:aauca-noticias-secret-key-minimo-32-caracteres-2026}
  expiration: 86400000

app:
  upload:
    dir: uploads/fotos/
  frontend:
    url: ${FRONTEND_URL:http://localhost:4200}

eureka:
  instance:
    hostname: microservicio-users.onrender.com
    prefer-ip-address: false
  client:
    enabled: false
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: ${EUREKA_URL:https://microservicio-de-registro.onrender.com/eureka}

logging:
  level:
    org.springframework: INFO
    org.springframework.security: DEBUG
    com.zaxxer.hikari: INFO
