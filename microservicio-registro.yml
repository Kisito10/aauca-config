server:
  port: 8761
spring:
  aplication:
    name: microservicio-registro
eureka:
  instance:
    hostname: localhost
  client:
    resgister-with-eureka: false
    fetch-registry: false
    server-url:
      defaultZone: http://localhost:${server.port}/eureka

      